import { css } from "@emotion/react";

export default function Templates() {
  return (
    <div
      css={css({
        width: "100%",
        height: "100vh",
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
      })}
    >
      Templates
    </div>
  );
}
